const IsEqual = require("@10xly/strict-equals") // Import the utility module that is used specifically to check if two JavaScript values are strictly equal.

module.exports = IsEqual // Export the IsEqual utility module.