const UncurryThis = require("uncurry-x") // Import the general utility module that is used specifically to uncurry JavaScript functions.

module.exports = UncurryThis // Export the general utility module that is used specifically to uncurry JavaScript functions.